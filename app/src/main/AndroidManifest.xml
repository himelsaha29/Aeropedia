<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.himel.aeropedia">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the "MyLocation" functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Aeropedia">
        <activity android:name=".boeing.Boeing777"></activity>
        <activity android:name=".boeing.Boeing767" />
        <activity android:name=".boeing.Boeing747" />
        <activity android:name=".boeing.Boeing757" />
        <activity android:name=".boeing.Boeing737" />
        <activity android:name=".manufacturers.Boeing" />
        <activity android:name=".antonov.AntonovAn225Mriya" />
        <activity android:name=".antonov.AntonovAn22Antei" />
        <activity android:name=".antonov.AntonovAn72Cheburashka" />
        <activity android:name=".manufacturers.Antonov" />
        <activity android:name=".antonov.AntonovAn124Ruslan" />
        <activity android:name=".airbus.AirbusBeluga" />
        <activity android:name=".airbus.AirbusA220" />
        <activity android:name=".airbus.AirbusA300" />
        <activity android:name=".airbus.AirbusA310" />
        <activity android:name=".airbus.AirbusA318" />
        <activity android:name=".airbus.AirbusA319" />
        <activity android:name=".airbus.AirbusA320" />
        <activity android:name=".airbus.AirbusA320neoFamily" />
        <activity android:name=".airbus.AirbusA321" />
        <activity android:name=".airbus.AirbusA330neo" />
        <activity android:name=".airbus.AirbusA330" />
        <activity android:name=".airbus.AirbusA340" />
        <activity android:name=".airbus.AirbusA380" />
        <activity android:name=".firebase.Firebase" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity android:name=".flightmap.FlightMap" />
        <activity android:name=".alexa.AlexaActivity" />
        <activity android:name=".manufacturers.ManufacturerMenu" />
        <activity android:name=".airbus.AirbusA350" />
        <activity android:name=".manufacturers.Airbus" />
        <activity android:name=".home.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".alexa.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data
                    android:host="www.amazon.com"
                    android:pathPattern="/ap/oa"
                    android:scheme="https" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.amazon.identity.auth.device.workflow.WorkflowActivity"
            android:allowTaskReparenting="true"
            android:launchMode="singleTask"
            android:theme="@android:style/Theme.NoDisplay">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <!-- android:host must use the full package name found in Manifest General Attributes -->
                <data
                    android:host="${applicationId}"
                    android:scheme="amzn" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>